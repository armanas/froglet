# TODO

The working launch, operator, and services backlog now lives in the private
`froglet-services` workspace.

That launch gate is explicit there: MVP is not considered ready until the main
site, public GitHub/release surface, hosted Froglet environment, hosted
temporary-identity environment, default marketplace, launch payment rails, and
claimed integrations are all working and verified.

This public repo keeps the protocol/runtime implementation, public
integrations, public docs, and release surfaces. Public follow-up work should
be tracked through GitHub issues, pull requests, or docs that live with the
relevant code.
