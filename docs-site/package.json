{
  "name": "docs-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run build:verifier && node scripts/generate-og.mjs && astro build",
    "preview": "astro preview",
    "preview:workers": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "astro": "astro",
    "test": "vitest --run",
    "build:verifier": "node scripts/build-verifier.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.3.1",
    "@astrojs/starlight": "^0.42.3",
    "astro": "^7.3.5",
    "rehype-mathjax": "^7.1.0",
    "remark-math": "^6.0.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.0",
    "jsdom": "^29.0.2",
    "lightningcss": "^1.32.0",
    "satori": "^0.13.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.83.0"
  }
}
