---
title: "The Deal Flow"
description: Six artifacts, one evidence chain.
---

import ChainCanvasInit from '../../../components/ChainCanvasInit.astro';
import DealFlowSequenceInit from '../../../components/DealFlowSequenceInit.astro';

## The chain

<div class="plot-shell">
<div class="plot-header">
  <div>
    <p class="plot-title">Click the chain or use the buttons below</p>
    <p class="plot-subtitle">Every artifact references the previous one by hash. Tamper with any link and the chain breaks.</p>
  </div>
</div>
<div class="plot-metrics">
  <div class="plot-metric"><strong>5 provider signatures</strong><span>Descriptor, offer, quote, invoice bundle, and receipt come from the provider.</span></div>
  <div class="plot-metric"><strong>1 requester signature</strong><span>The deal is the requester's cryptographic commitment to pay.</span></div>
  <div class="plot-metric"><strong>1 terminal proof</strong><span>The receipt is the final artifact any third party can verify later.</span></div>
</div>
<canvas id="chain-canvas" class="plot-canvas" width="700" height="120" role="img" aria-label="Interactive artifact chain: descriptor, offer, quote, deal, invoice, receipt" style="max-width:700px;height:120px;cursor:pointer"></canvas>
<noscript>
  <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;justify-content:center;padding:12px 0">
    <span style="color:#a78bfa">Descriptor</span> →
    <span style="color:#818cf8">Offer</span> →
    <span style="color:#67e8f9">Quote</span> →
    <span style="color:#93c5fd">Deal</span> →
    <span style="color:#fbbf24">Invoice</span> →
    <span style="color:#4ade80">Receipt</span>
  </div>
  <p class="plot-note">Each artifact references the previous one by SHA-256 hash. Enable JavaScript for the interactive version.</p>
</noscript>
<div id="chain-buttons" class="plot-button-row"></div>
<div id="chain-detail" class="learn-card" style="margin-top:12px;font-size:13px;line-height:1.6;color:#9ca3af;min-height:60px"></div>
<p class="plot-note">The artifact chain is both the protocol flow and the audit log. The same objects used during execution are the objects you verify afterwards.</p>
</div>

<ChainCanvasInit />

## Message exchange

<div class="plot-shell">
<div class="plot-header">
  <div>
    <p class="plot-title">Requester ↔ Provider message flow</p>
    <p class="plot-subtitle">Watch the four-step exchange that turns a quote into a verifiable receipt.</p>
  </div>
</div>
<canvas id="deal-flow-sequence" class="plot-canvas" width="520" height="240" role="img" aria-label="Animated sequence diagram: requester sends request quote, provider returns signed quote, requester sends signed deal, provider returns execute and receipt" style="max-width:520px;height:240px"></canvas>
<noscript>
  <ol style="padding-left:1.2em;color:#9ca3af;font-size:13px">
    <li>Requester → Provider: request quote</li>
    <li>Provider → Requester: signed quote</li>
    <li>Requester → Provider: signed deal</li>
    <li>Provider → Requester: execute + receipt</li>
  </ol>
  <p class="plot-note">Enable JavaScript to see the animated sequence diagram.</p>
</noscript>
<button id="deal-flow-sequence-btn" class="plot-button" type="button" aria-label="Pause animation">⏸ Pause</button>
</div>

<DealFlowSequenceInit />

## Key insight: who signs what

| Artifact | Signed by | Why |
|----------|-----------|-----|
| Descriptor | **Provider** | Declares identity |
| Offer | **Provider** | Commits to pricing |
| Quote | **Provider** | Prices specific workload |
| Deal | **Requester** | Commits to pay |
| InvoiceBundle | **Provider** | Payment instructions |
| Receipt | **Provider** | Proof of execution |

The **Deal** is the only artifact signed by the requester — it's their cryptographic commitment to pay. Everything else is signed by the provider.

:::tip[The receipt is proof]
Anyone holding the receipt can verify: the provider signed it, the result hash matches, the settlement is correct, the chain is unbroken. No dispute resolution — the math is the arbiter.
:::

## The signed artifact envelope

Every artifact in the chain is a JSON envelope with identity and integrity proof built in:

```json
{
  "artifact_type": "offer",
  "schema_version": "froglet/v1",
  "signer": "02a8d6...public_key",
  "created_at": 1700000000,
  "payload_hash": "sha256_of_canonical_payload",
  "hash": "sha256_of_canonical_signing_bytes",
  "payload": { ... },
  "signature": "bip340_schnorr_signature"
}
```

Verification: canonicalize the payload (RFC 8785 JCS), hash it with SHA-256, rebuild the signing bytes, verify the BIP340 Schnorr signature against the signer key. If it passes, the payload is untampered and the artifact came from the holder of the private key.
