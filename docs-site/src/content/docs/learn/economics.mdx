---
title: Trust & Economics
description: What is cryptographically guaranteed today, what is operator policy, and the staked-trust design on the roadmap.
---

import ProfitChartInit from '../../../components/ProfitChartInit.astro';
import TrustThresholdInit from '../../../components/TrustThresholdInit.astro';

## The trust model, layer by layer

Froglet separates three kinds of trust. Mixing them up is how marketplaces
overclaim, so the protocol is explicit about which layer each guarantee
lives in:

| Guaranteed by cryptography | Operator policy (hosted marketplace) | Not guaranteed by anyone |
|---|---|---|
| Artifact signatures (BIP340) | Listing and indexing | Execution **correctness** |
| SHA-256 hashes and the chain | Suspension via the [arbiter](#disputes-today-the-arbiter) | Provider competence |
| Lightning settlement state (payment preimage) | Complaint handling | Refund of the base fee on failure |
| Identity continuity (same key = same actor) | Identity attestation (DNS / OAuth domain claims) | Future behavior of any key |

### Trust signals today

The live marketplace exposes a **receipt-derived trust summary** per provider:
success count, failure count, rejected/canceled count, and total settled value,
computed from the signed receipts the indexer has verified. Two honest caveats:

- **Receipt history can be self-dealt.** A provider can run deals against
  itself to inflate counts. Settled-value totals make this cost real sats in
  routing fees, but the signal is still a *signal*, not proof. Weigh it
  together with attestation and account age, not alone.
- **Domain attestation raises sybil cost.** A provider that has proven control
  of a DNS domain or OAuth identity is more expensive to recreate after
  misbehavior than a fresh keypair. Filter for attested providers
  (`?attested=true`) when the stakes matter.

### Disputes today: the arbiter

The hosted marketplace runs a complaint-and-enforcement service. Anyone can
file a complaint with evidence against a provider; verdicts can suspend the
provider from marketplace listings. Be clear about what this is:

:::caution[Operator-adjudicated, by design — for now]
The MVP arbiter is **operator-adjudicated**. It is not decentralized,
stake-backed, panel-selected, or slashable. Suspension hides a provider from
the hosted marketplace's read API; it does not invalidate signed artifacts and
it is not a protocol-level rule. Deposits, public adjudicator registration,
appeals, panels, and slashing are post-MVP work. See
[ARBITER.md](https://github.com/armanas/froglet/blob/main/docs/ARBITER.md) for
the full contract.
:::

The protocol itself requires no trusted registry — you can verify any
provider's artifacts and run deals against it directly, marketplace or not.
The hosted marketplace and arbiter are convenience layers run by an operator,
and they are described here as exactly that.

### What a receipt proves — and what it doesn't

A signed receipt proves: the provider signed this outcome, the artifact chain
is unbroken, the result hash is what the provider committed to, and (on the
Lightning rail) the settlement state is cryptographically attested by the
payment preimage.

A receipt does **not** prove the result is *correct*. Verifying execution
correctness without re-running the work is an open research problem
(re-execution, trusted hardware, zero-knowledge proofs). Froglet's position:
the receipt makes outcomes *attributable and auditable* — a provider that
signs wrong results signs the evidence against itself — but detection still
requires someone to check. For deterministic workloads, spot-check by
re-running a sample on a second provider.

### Practical buyer guidance

- Keep individual deal sizes small relative to the trust evidence you have.
- Configure your node's requester spend caps so a runaway agent can't drain a
  wallet (see [Configuration](https://github.com/armanas/froglet/blob/main/docs/CONFIGURATION.md)).
- The **base fee is non-refundable** if execution fails — that is the
  provider's compensation for reserved resources. Only the success fee is
  conditional. Size base fees accordingly when you buy.
- Lightning payments are irreversible; Stripe payments follow card-network
  dispute rules. Pick the rail that matches your recourse expectations.

---

## Design roadmap: staked identity

The long-term design adds a fourth trust signal: **non-refundable stake**
bound to a provider identity. A provider deposits real money into its identity
through the marketplace; the stake is public, already-spent, and the identity's
listing value is destroyed if the operator (or, post-MVP, an adjudication
mechanism) upholds cheating against it. Self-dealing cannot inflate this
signal, because only non-refundable deposits move it.

:::note[Status: designed, not live]
The stake schema and endpoints exist in the marketplace codebase but are
**disabled pending settlement-backed stake receipts** — a stake must be proven
by a verifiable payment before it counts. Nothing below is a live guarantee
yet.
:::

The design only works if three assumptions hold, and we state them plainly:

1. **Cheating must be detectable and publicly provable.** For "provider never
   delivered," receipts and deadlines make this checkable. For "provider
   returned a wrong result," it requires re-execution or verifiable compute —
   see the receipt caveat above.
2. **Someone must adjudicate.** Stake is a bond, not a mechanism. Today the
   adjudicator would be the marketplace operator; making adjudication
   stake-backed and panel-based is the post-MVP path.
3. **Exposure is concurrent.** A cheating provider can run many deals in
   parallel before detection propagates, so per-deal arithmetic alone
   understates risk. The relevant comparison is stake versus *total concurrent
   exposure within the detection window*, which is why deal-size caps and
   small-first defaults matter even with stake.

With those assumptions, a requester heuristic falls out: require
`stake / exposure > k` for a risk threshold `k` of your choosing — where
`exposure` is everything you have outstanding with that provider, not just
this deal.

### Interactive: trust threshold check (design preview)

<div class="plot-shell">
<div class="plot-header">
  <div>
    <p class="plot-title">Set your risk threshold and see which deals pass</p>
    <p class="plot-subtitle">Adjust k to see which example stake-to-exposure ratios satisfy <code>stake / exposure &gt; k</code>. Green bars pass the safety check, red bars fail.</p>
  </div>
</div>
<div class="plot-controls">
  <label class="plot-control">risk threshold k: <span id="threshold-val">1.0</span> <input id="threshold-slider" type="range" min="1" max="100" value="10" style="width:180px;accent-color:#fbbf24" /></label>
</div>
<div class="plot-metrics">
  <div class="plot-metric"><span id="threshold-result">Loading…</span></div>
</div>
<canvas id="trust-threshold" class="plot-canvas" width="600" height="302" role="img" aria-label="Interactive trust threshold diagram showing which stake-to-exposure ratios pass the safety check" style="max-width:600px;width:100%"></canvas>
<noscript><p class="plot-note">Enable JavaScript to use the interactive trust threshold diagram.</p></noscript>
<p class="plot-note">Each bar represents an example deal. Bar length = stake/exposure ratio. The dashed line marks your threshold k. Design preview — stake is not live yet.</p>
</div>

<TrustThresholdInit />

---

## Design note: the deal as a game

The fee structure is live today and worth understanding formally. One modeling
assumption applies throughout: *outcomes are taken from signed receipts and
requester acceptance*. The model prices reliability incentives; it does not
prove correctness (see above).

### Definitions

Let $\mathcal{N}$ be the set of all froglet nodes. Each node $n \in \mathcal{N}$ has a secp256k1 keypair $(k_n, P_n)$ where $P_n = k_n \cdot G$.

A **service** $s$ is characterized by:

$$s = (\text{offer\_kind}, \text{runtime}, b_s, f_s, \mathbf{L}_s)$$

where $b_s$ is the base fee, $f_s$ is the success fee, and $\mathbf{L}_s = (t_{\max}, m_{\max}, \phi_{\max})$ is the execution limit vector (time, memory, fuel).

A deal between requester $R$ and provider $P$ is a two-player sequential game:

$$\text{Quote} \xrightarrow{\text{P signs}} \text{Deal} \xrightarrow{\text{R signs}} \text{Execute} \xrightarrow{\text{P runs}} \text{Receipt} \xrightarrow{\text{P signs}}$$

### Payoffs

Let $v$ be the value to the requester, $c$ the provider's cost, $p = b_s + f_s$ the total price.

| Outcome | Requester | Provider |
|---------|-----------|----------|
| Success | $v - p$ | $p - c$ |
| Failure | $-b_s$ | $b_s - c$ |
| No deal | $0$ | $0$ |

Note the failure row: the requester eats the base fee. That is deliberate
(it compensates reserved resources and deters free-riding) and it is why the
base fee should stay small relative to the success fee.

### Participation constraints

Requester participates if $v \geq b_s + f_s$. Provider participates if $b_s + f_s \geq c$. Gains from trade must be positive: $v - c > 0$.

### Two-leg settlement analysis

Let $q \in [0, 1]$ be the probability the provider successfully executes.

**Requester expected payoff:**

$$\mathbb{E}[\pi_R] = q(v - b_s - f_s) + (1-q)(-b_s) = qv - b_s - qf_s$$

**Provider expected payoff:**

$$\mathbb{E}[\pi_P] = q(b_s + f_s - c) + (1-q)(b_s - c) = b_s + qf_s - c$$

### Incentive compatibility

The provider is incentivized to maximize $q$:

$$\frac{\partial \mathbb{E}[\pi_P]}{\partial q} = f_s > 0$$

The success fee acts as the provider's marginal incentive for reliability.
Higher $f_s$ = stronger incentives for *delivering outcomes the requester
accepts* — which is the observable proxy for quality this model can price.

### Interactive: provider profit vs quality

<div class="plot-shell">
<div class="plot-header">
  <div>
    <p class="plot-title">Move the fees and watch the incentive line change</p>
    <p class="plot-subtitle">The slope of provider profit with respect to quality is the success fee. That is why the green line gets steeper when success fees rise.</p>
  </div>
</div>
<div class="plot-controls">
  <label class="plot-control">base fee: <span id="bf-val">3</span> sat <input id="bf-slider" type="range" min="0" max="10" value="3" style="width:120px;accent-color:#f87171" /></label>
  <label class="plot-control">success fee: <span id="sf-val">5</span> sat <input id="sf-slider" type="range" min="0" max="15" value="5" style="width:120px;accent-color:#4ade80" /></label>
  <label class="plot-control">cost: <span id="c-val">2</span> sat <input id="c-slider" type="range" min="0" max="8" value="2" style="width:120px;accent-color:#fbbf24" /></label>
</div>
<div class="plot-metrics">
  <div class="plot-metric"><strong id="profit-at-zero">1.0 sat</strong><span>provider payoff at q = 0</span></div>
  <div class="plot-metric"><strong id="profit-at-one">6.0 sat</strong><span>provider payoff at q = 1</span></div>
  <div class="plot-metric"><strong id="break-even-q">0.00</strong><span>break-even quality threshold</span></div>
</div>
<canvas id="profit-chart" class="plot-canvas" width="600" height="220" role="img" aria-label="Interactive profit chart: provider profit and requester payoff as functions of provider quality q" style="max-width:600px;height:220px"></canvas>
<noscript><p class="plot-note">Enable JavaScript to use the interactive profit chart.</p></noscript>
<p class="plot-note">X = provider quality (q). Green = provider profit. Blue = requester payoff with requester value fixed at 10 sat.</p>
</div>

<ProfitChartInit />

### Optimal fee structure

With total price $\bar{p}$ fixed, the equilibrium split for a risk-neutral provider with reliability $q^*$:

$$b_s^* = \frac{c}{1 + q^*}, \quad f_s^* = \bar{p} - b_s^*$$

This ensures the provider breaks even on failed executions while the success fee captures the surplus.

## Network economics

### Marketplace viability

The marketplace charges $(b_M, f_M)$ per search. With query rate $\lambda$ and $N_P$ indexed providers:

$$\text{Rev}_M = \lambda \cdot (b_M + q_M f_M)$$

$$C_M = c_{\text{idx}} \cdot N_P + c_{\text{qry}} \cdot \lambda$$

Viable when $\text{Rev}_M > C_M$. (The current hosted marketplace charges
nothing — this models how an operator could sustain one.)

### Provider discovery value

A provider's expected value from marketplace registration:

$$\Delta V_P = \lambda_M \cdot q_P \cdot (b_P + f_P - c_P) - \text{registration cost}$$

Rational when $\Delta V_P > 0$.

### Network effects

The marketplace exhibits cross-side network effects. Steady-state:

$$N_P^* = g_P(N_R^*), \quad N_R^* = g_R(N_P^*)$$

where $g_P$ and $g_R$ are increasing. Two equilibria: trivial $(0, 0)$ and a non-trivial fixed point where both sides participate.

## Welfare analysis

Total welfare:

$$W = \sum_{\text{deals}} (v_i - c_i) - \text{transaction costs}$$

The protocol pushes $W$ up by:

1. **Reducing information asymmetry** — signed receipts make outcomes attributable
2. **Reducing counterparty risk on settlement** — HTLCs bind payment to acceptance
3. **Reducing search costs** — the marketplace aggregates information
4. **Enabling price discovery** — competition drives prices toward cost

In the limit of a large market:

$$\lim_{|\mathcal{N}| \to \infty} p_s \to c_s + \epsilon$$

:::tip[The honest economic conclusion]
Froglet makes commitments and outcomes *signed, attributable, and auditable*,
and binds Lightning settlement to acceptance cryptographically. Execution
correctness and provider quality remain market problems — addressed today by
receipt-derived signals, attestation, small-deal discipline, and an
operator-adjudicated arbiter; addressed tomorrow, if the roadmap holds, by
settlement-backed stake.
:::
