<button type="button" class="froglet-theme-toggle" data-froglet-theme-toggle>
	<span class="froglet-theme-toggle__track" aria-hidden="true">
		<span class="froglet-theme-toggle__sun">◐</span>
		<span class="froglet-theme-toggle__moon">◑</span>
	</span>
	<span class="froglet-theme-toggle__label" data-froglet-theme-label>light</span>
</button>

<style is:global>
	.froglet-theme-toggle {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.42rem 0.72rem 0.42rem 0.46rem;
		border: 1px solid hsl(var(--froglet-border));
		border-radius: 999px;
		background: color-mix(in srgb, hsl(var(--froglet-card)) 92%, transparent);
		color: hsl(var(--froglet-muted));
		font: 600 0.78rem/1 var(--froglet-font-code);
		letter-spacing: 0.02em;
		cursor: pointer;
		transition:
			border-color 140ms ease,
			background-color 140ms ease,
			color 140ms ease,
			transform 140ms ease;
	}

	.froglet-theme-toggle:hover {
		border-color: hsl(var(--froglet-border-strong));
		color: hsl(var(--froglet-foreground));
		transform: translateY(-1px);
	}

	.froglet-theme-toggle__track {
		display: inline-grid;
		grid-template-columns: 1fr 1fr;
		align-items: center;
		width: 2rem;
		padding: 0.12rem;
		border-radius: 999px;
		background: hsl(var(--froglet-surface));
		color: hsl(var(--froglet-foreground));
	}

	.froglet-theme-toggle__sun,
	.froglet-theme-toggle__moon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 0.8rem;
		height: 0.8rem;
		font-size: 0.65rem;
		border-radius: 999px;
		transition:
			background-color 140ms ease,
			color 140ms ease,
			transform 140ms ease,
			opacity 140ms ease;
	}

	.froglet-theme-toggle[data-mode='light'] .froglet-theme-toggle__sun,
	.froglet-theme-toggle:not([data-mode]) .froglet-theme-toggle__sun {
		background: hsl(var(--froglet-accent));
		color: hsl(var(--froglet-accent-foreground));
	}

	.froglet-theme-toggle[data-mode='dark'] .froglet-theme-toggle__moon {
		background: hsl(var(--froglet-accent));
		color: hsl(var(--froglet-accent-foreground));
	}

	.froglet-theme-toggle__label {
		min-width: 2.8rem;
		text-align: left;
		text-transform: lowercase;
	}
</style>
