services:
  discovery:
    build:
      context: .
      target: discovery
    image: froglet-discovery:local
    environment:
      FROGLET_DISCOVERY_DB_PATH: /data/discovery.db
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./data:/data
